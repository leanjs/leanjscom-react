import { BackgroundImage } from 'rebass'

import styled from 'styled-components'

const NoRepeatBackgroundImage = styled(BackgroundImage)`
  background-repeat: no-repeat;
`

export default NoRepeatBackgroundImage
